# Rust build output
/src-tauri/target/
# Tauri-generated schemas/artifacts (regenerated by tauri-build)
/src-tauri/gen/
# Node
node_modules/
# Lockfiles are intentionally NOT ignored once generated; commit them on first
# successful build so versions are reproducible.
