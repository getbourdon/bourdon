# v0.6.0 recovery notes (May 2026)

Operational snapshot produced after cloning and reconciling Git tags, `main`, and GitHub PR state. Use this as the short **delta report** when returning from interrupted work sessions.

## Version and releases

| Ref | Points to | Notes |
|-----|-----------|--------|
| `v0.6.0` tag | Release commit `e98871f` | **Bidirectional federation** — write-side MCP, unified dedupe, default access env vars (see `#54`). |
| `main` HEAD | Ahead of tag | Includes post-release commits (e.g. `.web`/`docs` PRs `#59`–`#60`). `pyproject.toml` still declares **`0.6.0`** until a deliberate version bump (`0.6.1+`). |
| `pyproject.toml` | `version = "0.6.0"` | Matches labelled feature set when used with MCP `commit_to_federation`. |

Nothing in this snapshot automatically re-tags releases; tagging `v0.6.1` is a conscious release-owner action once PRs land.

## Pull request `#61`: Codex live SQLite (`read live state sqlite for recognition`)

- **Relationship to `main`:** merge-base between `pull/61/head` and `main` is **current `main` tip**. The branch adds **exactly one commit on top**, `87e7498` — _fix(codex): read live state sqlite for recognition_.
- **`git diff main..pull/61/head`** (+721 −42): focused on **`participants/codex.py`**, **CLI glue** (`cli/main.py`), **`tests/test_codex_participant.py`**, **`tests/test_cli.py`**, and narrow doc/test expectation updates (**`README.md`**, **`docs/agent-integration-status.md`**, etc.).
- **Scope verdict:** Single concern (`fix(codex): …`). **No split required** unless review finds participant surface-area too large — then refactor can follow in a dependent PR.
- **Action:** Prefer **Merge** `#61` (or rebase the branch onto latest `main` and fast-forward-merge) rather than stacking unrelated chores into the same branch.

Naming for this slice: **Recognition path for Codex** reading **live** `state_*.sqlite` (not stale snapshots) — aligns with MCP **recognition-first** tooling (`prepare_recognition_context`), which ultimately consumes materialized federation state Codex participants feed.

## Install / MCP wrapper map (“where wires live”)

| Layer | What it is |
|-------|-------------|
| **PyPI/package** | `bourdon = "cli.main:main"` · `requires-python ">=3.10"` · deps in [`pyproject.toml`](../pyproject.toml). Extra **`[server]`** pulls `fastmcp` + `bourdon serve` / `python -m core.l6_server`. |
| **L6 MCP process** | `python -m core.l6_server` with **`--library`** (defaults under user home federation root). Stdio/`--transport` used by Cursor, Claude Desktop, OpenManus, etc. Implementation: [`core/l6_server.py`](../core/l6_server.py). |
| **Thin CLI wrapper** | `bourdon serve` — banner-friendly launcher with same knobs as `-m`. |
| **Recognition CLIs** | `bourdon prepare-turn` maps to federation recognition; **`bourdon codex prepare-turn`** is Codex-specific (PR `#61` tightens SQLite freshness). See [`cli/main.py`](../cli/main.py). |
| **Env knobs** | e.g. `BOURDON_DEFAULT_ACCESS_LEVEL` (install-wide default visibility), library path overrides per integration doc. |

First-time operators should follow **`docs/integrations/*.md`** and **`bourdon doctor`** rather than improvising MCP JSON.

### L6 MCP tools (v0.6.0+)

Seven tools registered on the federation server:

`query_agent_memory`, `list_recent_work`, `find_entity`, `commit_to_federation`, `get_cross_agent_summary`, `prepare_recognition_context`, `get_deeper_context`.

## Minimal smoke expectations

1. **`bourdon dogfood`** — real-store round-trip matrix (already documented in [`README.md`](../README.md) / [`AGENTS.md`](../AGENTS.md)).
2. **`scripts/mcp_smoke_test.py`** — stdio MCP smoke. **`--isolate-federation-write-smoke --library-path`** (temporary empty directory) runs every tool—including **`commit_to_federation → find_entity` read-back**—without demo-seeded manifests. Omit the isolate shorthand when asserting against populated `~/agent-library` demos.


## Branch / PR hygiene (effective immediately)

Formalized in [`development-workflow.md`](development-workflow.md): branch from **`main`** with prefixes like `feature/` or `fix/`, **one PR per coherent slice**, merge `#61` on its own.

## Canonical clone used for recon

Local path used during inventory: **`C:\\Users\\cumul\\dev\\bourdon`** (feature branch **`feature/v0-recovery-mcp-write-smoke-and-docs`** carries this docs + MCP smoke augmentation). Open that directory as the Cursor project root when proceeding.
